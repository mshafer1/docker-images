# Docker image building repo

This repo exists to create and build/publish Docker images

# Why?

Well, a couple of reasons, but the short version is to consolidate a list of apps that I needed in Docker into a single repo to have one pipeline build and publish.
